<h1 class="text-center bebas-neue-regular">All lists</h1>

<div class="container d-flex justify-content-center">
  <ul class="list-group list-group-flush" id="lists">
    <% @lists.each do |list| %>
      <%# <%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY", width: 400, height: 300, crop: :fill) %>
      <li class="list-group-item">
        <%= link_to list.name, list_path(list), class: "text-decoration-none"%>
      </li>
    <% end %>
  </ul>
</div>


<div class="text-center">
  <%= link_to "Add a list", new_list_path, class: "btn btn-primary" %>
</div>
<p>test</p>
